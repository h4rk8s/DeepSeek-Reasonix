// Package transcript defines the frontend-neutral semantic transcript contract.
// It classifies durable history and live events without choosing terminal, web,
// or desktop rendering. Provider-visible messages remain owned by agent; these
// entries are display-only projections and never enter the prompt prefix.
package transcript
